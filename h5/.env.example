# 本地后端通过 Vite /api 代理访问，部署时改为完整的公网 HTTPS 地址。
VITE_API_BASE_URL=/api/v1

# 本地需要联调公网环境时配置；不配置则代理到 127.0.0.1:3000。
VITE_API_PROXY_TARGET=https://api.example.com

# 仅本地开发使用。目标必须与服务端签名 URL 的 COS Origin 完全一致。
VITE_COS_PROXY_TARGET=https://bucket.cos.region.myqcloud.com
